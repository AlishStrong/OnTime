rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    match /companies/{company} {
      allow create: if request.auth != null && request.resource != null;
      allow list: if request.auth != null;
    }
  }
}
